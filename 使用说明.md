# NFC 生日祝福网页

NFC 芯片保存的是网页链接，因此需要先把整个 `birthday-nfc` 文件夹发布到互联网，再把获得的 HTTPS 地址写入 NFC 标签。

页面开启后，点击蛋糕会触发蛋糕冲向屏幕的动画，随后显示“给你的话”文字页。

## 修改内容

用文本编辑器打开 `index.html`，搜索 `const CONFIG`：

```js
const CONFIG = {
  name: "收礼人的名字",
  from: "你的名字",
  message: "动画页的一句话祝福",
  letter: `这里填写文字页正文。

空一行就会开始新的段落。`
};
```

`letter` 使用反引号包围，可以直接换行。不要删除开头和结尾的反引号。

也可以使用网址参数临时替换内容：

```text
https://你的网址/?name=小美&from=小张&message=天天开心&letter=这里是想说的话
```

较长的文字建议直接修改 `CONFIG.letter`，避免 NFC 链接过长。

## 发布网页

1. 打开 `https://app.netlify.com/drop`。
2. 把整个 `birthday-nfc` 文件夹拖入页面。
3. 发布完成后复制生成的 `https://...netlify.app` 地址。
4. 在手机浏览器中打开并完整测试动画、音乐和文字页。

也可以使用 GitHub Pages、Cloudflare Pages、Vercel 或自己的服务器。请上传整个 `birthday-nfc` 文件夹，页面使用到的 GIF、WebP 和 PNG 素材都要保留。

## 写入 NFC

1. 在手机安装 `NFC Tools`。
2. 进入“写入”，选择“添加记录”。
3. 选择“自定义 URL/URI”，粘贴完整的 HTTPS 地址。
4. 点击“写入”，将手机 NFC 感应区贴近芯片。
5. 使用另一台手机碰触测试，确认无误后再考虑锁定标签。

NTAG213 已足够保存普通网页链接。标签不要直接贴在金属表面；金属场景请使用抗金属 NFC 标签。

## 角色素材来源

- `meiji-notepad.png`：取自哔哩哔哩账号“美叽和大鼠”发布的《美叽和大鼠迷你2》封面，已处理为透明贴纸。原始页面：`https://www.bilibili.com/video/BV14iCaYDEYr`
- 目录内新增的 4 张 WebP 与 3 个 GIF：用户自行整理提供，作为散落贴纸分布在第二个“生日快乐”界面的蛋糕周围。
- 后续新增的 10 个 GIF：用户自行整理提供，已加入第二个界面的蛋糕周围；原本带场景背景的两张素材使用 `meiji-pink-cutout.webp` 与 `meiji-blue-cutout.webp` 透明动画版本，原始 GIF 保留备用。
- `meiji-rat.gif` 与 `birthday-mascot.png`：旧版备用素材，当前页面未引用。

角色素材版权归原作者所有。本页面建议仅用于个人、非商业的生日祝福；公开传播或商业使用前应取得权利方许可。
